{
  "id": "extender:dark-map",
  "name": "Dark Map",
  "description": "Applies a dark filter to the map. On Leaflet this only affects the map tiles, leaving your markers untouched; Google Maps has no equivalent hook, so there the whole map — including markers and controls — is filtered.",
  "version": "1.0.1",
  "author": "Map Extender",
  "usage": "Always-on map filter",
  "matchPatterns": [
    "*://*/*"
  ],
  "settingsSchema": [
    {
      "key": "brightness",
      "label": "Brightness (0.3–1.5)",
      "type": "number",
      "default": 0.9
    },
    {
      "key": "contrast",
      "label": "Contrast (0.5–2)",
      "type": "number",
      "default": 1.05
    }
  ],
  "settings": {
    "brightness": 0.9,
    "contrast": 1.05
  },
  "screenshots": []
}
